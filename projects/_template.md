---
title: Project Name
summary: One-line description shown on the Projects index.
status: draft
order: 99
---

# Project Name — Project Overview

Write the project overview here. The same markdown features as the rest of the
site are available: footnotes become Tufte sidenotes, `$math$` renders via
KaTeX, and the `:::aside` / `::figure{...}` directives work.

To start a new project: copy this file to `projects/<your-slug>.md` and edit it.
Files whose name starts with `_` are ignored by the build, so this template
never publishes. `order` controls position in the Projects list (lower first).
